<%= form_with(model: user , local: true) do |form| %>

  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div><%= form.label :name , "名前" %></div>
  <div><%= form.text_field :name %></div>

  <div><%= form.label :email , "メールアドレス" %></div>
  <div><%= form.text_field :email %></div>

  <div><%= form.label :password , "パスワード" %></div>
  <div><%= form.text_field :password %></div>

  <div><%= form.label :password_confirmation , "パスワード（確認）" %></div>
  <div><%= form.text_field :password_confirmation %></div>

  
  <div><%= form.submit buttun_name , id: user_form_id %></div>
  
<% end %>